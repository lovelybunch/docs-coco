---
title: "Introduction"
description: "Coconut Context: a solution platform built on pages"
icon: "coconut"
---

Coconut Context is a solution platform built on pages. A page is a markdown document: a person reads it in a browser, and an agent reads the same page over HTTP or MCP. Every page carries **structured metadata** (queryable key-value data) and lives in a **space** with real access control. Each space also has **one agent** that can run tasks on demand or on a schedule, using connectors to reach your other tools.

Put those pieces together and a wiki starts acting like an application:

- The **page body** is the human-readable narrative: an investment memo, a runbook, an account brief.
- The **metadata** is the state that actually changes: a stage, a score, a growing list of sources. Each key is patchable and audited on its own, and queryable across pages without opening the document.
- The **space agent** keeps both current, folding new information into the prose and updating the metadata, on demand or on a schedule.
- **Webhooks, export/import, and templates** connect a space to the rest of your stack, so a solution you build once is one you can repeat.

## Three ways to reach a page

Whether you're a person in a browser, an agent calling the HTTP API, or an MCP client, you hit the same resolver and the same access control. Three interfaces, one set of rules:

| Interface | Who uses it | How |
| --- | --- | --- |
| Browser | People | Rendered HTML at `/pages/:space/:path` |
| HTTP API | Agents and scripts | `Accept: text/markdown` (or `.md`) returns clean markdown; JSON for everything else |
| MCP | AI agents and IDEs | `context_*` tools over JSON-RPC, with OAuth or bearer auth |

You don't have to talk to the HTTP API by hand. The [Coconut CLI](https://github.com/lovelybunch/coconut-cli) (`npm i coconut`, then run it as `coco` or `coconut`) drives spaces, pages, metadata, and agents from a terminal or a CI job. The [Coconut SDK](https://github.com/lovelybunch/coconut-sdk) (`npm i coconut-sdk`) is the typed TypeScript client the CLI itself is built on: reach for it when a script or an agent needs more than the CLI gives you.

Pages are versioned in Postgres as append-only revisions, not a git lifecycle. Every write is a new revision, and reading an older one is one query parameter away.

## Explore the docs

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Run the stack, create a page, attach metadata, query across pages.
  </Card>

  <Card title="Concepts" icon="book" href="/concepts/spaces-and-pages">
    Organizations, spaces, pages, metadata, agents, templates, and more.
  </Card>

  <Card title="HTTP API" icon="code" href="/api-reference/overview">
    The routes agents and systems use to read and write.
  </Card>

  <Card title="Solutions" icon="lightbulb" href="/examples/deal-pipeline">
    Worked examples: PE pipelines, SaaS accounts, runbooks, contracts, suppliers.
  </Card>
</CardGroup>